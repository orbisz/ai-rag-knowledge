server:
  port: 8090

spring:
  ai:
    ollama:
      base-url: http://101.43.191.204:11434

# Redis
redis:
  sdk:
    config:
      host: 101.43.191.204
      port: 16379
      pool-size: 10
      min-idle-size: 5
      idle-timeout: 30000
      connect-timeout: 5000
      retry-attempts: 3
      retry-interval: 1000
      ping-interval: 60000
      keep-alive: true

logging:
  level:
    root: info
  config: classpath:logback-spring.xml
